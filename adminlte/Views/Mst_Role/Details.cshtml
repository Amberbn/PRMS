@model adminlte.Models.Mst_Role

@{
    ViewBag.Title = "Details";
}

<h3>Details Role</h3>

<div>
 
    <hr />
    <dl class="dl-horizontal">
        <dt>
           @Html.LabelFor(model => model.RoleName,"Role")
        </dt>

        <dd>
          @Html.DisplayFor(model => model.RoleName)
        </dd>

        <dt>
            @Html.LabelFor(model => model.IsActive, "Is Active")
        </dt>

        <dd>
          @Html.DisplayFor(model => model.IsActive)
        </dd>
        
       
        <dt></dt>
        <dd class="form-group"><br />
            <button type="button" value="Edit" class="btn btn-primary" onclick="location.href='@Url.Action("Edit", new { id = Model.RoleId })'">Edit</button>
            <button type="button" value="Back" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Mst_Role")'">Cancel</button>
        </dd>

    </dl>
</div>
